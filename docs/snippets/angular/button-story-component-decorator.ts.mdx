```ts
// button.stories.ts

import { Meta, Story, templateDecorator, componentDecorator } from '@storybook/angular';

import { ButtonComponent } from './button.component';
import { ParentComponent } from './parent.component';

export default {
  title: 'Example/Button',
  component: ButtonComponent,
  decorators: [
    moduleMetadata({
      declarations: [ParentComponent],
    }),
    // With template
    templateDecorator((stroy) => `<div style="margin: 3em">${story}</div>`),
    // With component which contains ng-content
    componentDecorator(ParentComponent),
  ],
} as Meta;

const Template: Story<ButtonComponent> = (args: ButtonComponent) => ({
  props: args,
  template: '<app-button label="Submit"></app-button>',
});

const Component: Story<ButtonComponent> = (args: ButtonComponent) => ({
  props: args,
});
```
