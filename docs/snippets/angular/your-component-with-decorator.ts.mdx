```ts
// your-component.stories.ts

import { Meta, templateDecorator } from '@storybook/angular';

export default {
  title: 'YourComponent',
  component: YourComponent,
  decorators: [templateDecorator((story) => `<div style="margin: 3em">${story}</div>`)],
} as Meta;
```
